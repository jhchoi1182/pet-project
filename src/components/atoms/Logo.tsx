import { ubuntu } from "@/app/layout";
import React from "react";

export default function Logo() {
  return <h1 className={`text-[42px] ${ubuntu.className} text-yellow select-none`}>StudySync</h1>;
}
